# 自动测试

插件使用 Node.js 内置测试器，不需要安装额外依赖。

在插件目录运行：

```powershell
npm test
```

当前覆盖（309 项 Node 测试，2026-09-06）：

本轮新增：

- Node：自动配图总闸旧配置默认开启，关闭只保留 `global_enabled` 并完整保留伙伴 `enabled/daily/task`；observer/express 总闸门禁、频率写入关闭时拒绝、主页开关和频率页只读锁定的静态契约；API 实链路覆盖关闭/恢复、配置保留和旧写入拒绝；隔离子进程验证 observer 无模型请求、express 不读取图库。
- Node：纸飞机自动定位使用宿主 `session:list.modified` 与最后真实用户消息的共同活跃时间，当前正在回复的会话不会被旧窗口抢走；会话列表接受 `visibility` 缺省/null，并与定位使用同一排序；`session:list`/`session:get` 请求有超时保护。
- Node：兼容 Hana 0.817+ `session:list` 缺少 `sessionId` 的配图反馈：通过 `session:get` 校验公开会话，保留显式会话 ID，并区分宿主 ID 与内部文件 UUID；新版兜底仍拒绝私密/插件私有会话。

- Node：配图聊天确认先落盘后保留短期会话状态，同一建议重复确认幂等成功；按图片 ID 回查当前标签，覆盖“回包丢失后仍已修改”的真实失败形态。
- Node：主卡片与图库确认异常时回查标签再决定是否报错；确认后的展柜刷新失败不倒灌成确认失败；主卡片新版 surface session 走请求头并保留旧版 query 兼容。
- Node：配图聊天思考耗尽时自动放大预算重试；最终失败透传 `session_id`，后续“继续”复用原聊天上下文；选定 DeepSeek Chat Completions 请求带 `reasoning_effort=low`。
- Node：主卡片与图库确认修改不依赖不稳定的 iframe `AbortSignal`，`/sticker` 页面禁止缓存旧版内联脚本；确认失败时保留建议卡并恢复按钮，不自动关闭聊天面板。

悬浮球新增：

- Node：原图 images 通道优先发送、标题/标签隐藏到 session:send context.afterUser、附件兜底不泄露语义、固定目标公开会话白名单、图片消息标题清洗、消息暗号组装、pinned 归一化、路径边界、正文清洗、requestId、最活跃会话选择；附件暗号优先显示原图、登记失败才退回原生 images；纸飞机识图入库 `tagged_at` 与旧图启动回填；曝光账本跨 decision-log/recent-match 去重、旧版账本修复/助手隔离/成功送达记账/新图优先探索；正反馈 image/context/both 替换与撤销、共享 mapping 增量回滚和失效冲突、根级旧偏好兼容、场景 clear 单次递减；express 决策日志串行写入；删除图片同步清理最近配图记录；另做本地伪总线 smoke，真实 Hana 渲染仍需实机验收。
- Python：纸飞机参考图透明素材/预置右上朝向/禁止额外线条/巡航/接近水平（轻微上扬 5.7°）点击冲刺与轻微颠簸/回航、动态气体尾流（波纹带+粒子流：平时若隐若现、悬停明显、冲刺拉平拉长成束）、背景流星、待机俯仰起伏、动效触发与衰减、拖拽优先级、系统减少动画、72px 最终尺寸绘制、右键菜单、普通面板点击空白关闭与延迟半透明、识图期间屏蔽右键菜单、弹窗锚点、屏幕边界、面板异步刷新、正文 payload、发送成功清空、失败保留与超时确认重发；
- Python/Node：纸飞机唯一白名单与旧配置归一化、连续 QPainter 动画门禁、配图送达尾流闪亮与衰减、全局光标滞回 hover、图片网格选中态、正文输入、回车发送、显式发送按钮、实体面板背景、左右面板并存的结构回归；
- Node：最近配图按 session 隔离、覆盖保留最后一张、只存语义字段不存绝对路径；公开非插件会话过滤、公共反馈逻辑正向/负向/撤销、助手与情绪隔离、稳定 mapping id 撤销定位、最近记录时间戳并发校验；纸飞机反馈与内嵌聊天代理端点静态契约。
- Python：最近配图区显示/隐藏、会话或配图变化触发尾流、喜欢/不喜欢状态、正反馈拆两键（喜欢=图本身、应景=配得好）状态同步与取消、配图手帐按对话框标题展示、手帐四按钮布局（喜欢/应景/不喜欢/聊一聊）、聊一聊只在点过不喜欢后出现、反馈失败后当前行按钮恢复、窄面板按钮自适应、内嵌聊天消息与建议预览/确认状态、旧回包丢弃与 daemon 网络线程退出安全。
- 页面交互：主页「自动配图」总闸与悬浮球开关、关闭总闸时频率页保留原值并锁定调整/保存、图库加入/移出按钮、数据与迁移页完整搬家入口、图库保留普通 ZIP 导入、导出目录原生选择按钮、设置页不再承载悬浮球配置
- 迁移 ZIP：零依赖 stored ZIP 写入/解析、`manifest.json` 与 `stickers.json` 白名单元数据、v2 搬家数据清洗、图片 SHA-256 重映射、助手 ID/名称映射、偏好/教学/应景/曝光/学我说话资料重映射、名称/描述/标签恢复、相对路径优先和文件名兜底匹配、重名歧义保护、危险路径拒绝、未知/未来 manifest 拒绝、旧 ZIP 无元数据兼容、缺失图片跳过、串行导入、临时文件原子改名、关闭中的自动配图总闸不被迁移包重新打开、普通图库入口与完整搬家入口分流、API 实链路 smoke、最近导出目录读写、PowerShell STA 目录选择/取消/非目录校验
- express 配图交付：Hana 0.679+ 走 `ui_only` image deferred；工具结果带标准 `mediaGeneration` 占位，宿主按 taskId 用 `sessionFiles` 原地替换，成功不再唤醒父助手且图片固定在当前回复；0.679 以下和未知宿主版本继续走 `details.card` iframe 卡片，避免硬切新版协议；旧 iframe route 仍保留显式宽高比、`hana.ready` 握手与 `ui.resize` 协议回归；成功发图后写入按 session 隔离的最近配图记录。
- 旧 iframe 卡片自适应二分（v0.33.1）：<400 贴原图 / ≥400 放大填满、关开关回退旧行为(≥200 放大填满 / <200 原尺寸)、非法输入不放大不崩溃（前后端共用 lib/smart-fit.js 的 fitDecision）
- 启动烟测：`ball/start → ready/health → 未授权请求拒绝 → ball/stop`。

Python 离屏测试在插件 `python` 目录下运行：

```powershell
$env:QT_QPA_PLATFORM = "offscreen"
python -m unittest test_ball_app.py -v
```


- Observer 两阶段频率抽样
- 表情包标签打分、偏好、否决与排除名单
- 批量识图任务重启时回收 `current_ids` / `current`
- 已取消任务不会被重启恢复逻辑改动
- 向量通道参与选图：无标签匹配时靠语义相似度选中（回归：少 `await` 导致向量通道静默失效）
- 向量 bonus 排序：相似度高的排前面
- execute 调用契约：向量通道原地修改 scored 并返回同一引用，候选必须非空（回归：v0.19.5 回填同一引用导致永远 no_match）
- 偏好按助手隔离：不合并其他助手的喜欢 / 反感记录（回归：遍历全部 users 导致串号）
- 英文问候词词边界：`this` / `while` / `something` 不误判 `hi`（回归：字符串包含误判）
- 标签清洗：识图/情绪词去除换行、引号、控制字符并限长
- 偏好收集：空情绪不收集任何偏好
- 方言库完整性：9 种方言、难度标注仅新疆话、例句加料
- 方言习惯式文案：身份化、锚定起头、质量声明、零指令词；新疆话普通版特制文案（内置整句示范）
- 方言加强版文案：九种方言全部有 personaAdvanced（身份化、情绪表、正事示例、零指令词）；二轮增量特征抽查（语序/音调/情绪）；四川话精修保留断言；台湾腔写中国台湾、新疆话无馕言文比喻
- 方言人格写入/移除/幂等/自愈/日志
- 方言配置归一化与迁移（老三档浓度→开关式、无效方言丢弃、v2 mode→v3 boost 迁移）
- 方言加强版 boost：配置保留/迁移、sync 写加强文案（回归：mode 改 boost 后 sync 传参）
- 方言动态回响：buildDialectEcho 身份化+双句示范+零指令词、isWorkTalk 正事判断（技术词命中/闲聊放行）、shouldBoostRound 频率衰减（前 8 条必注入后 60% 概率）；回响例句池九种方言齐全（句式级短句 ≤16 字、零指令词）
- 方言库完整性：10 种方言（9 种地方话 + 学我说话）、userstyle 无回响（回归：v0.30.0）
- 学我说话 v2（分通道提炼流水线，0.33.74）：
  - 统计基线：标点/句尾语气词/连续汉字短段短语/emoji 占比精确计算、句长中位数短消息占比、空输入安全兜底；短语统计只收 2~12 字短段，排除拼音英文碎片、跨边界字符、常见结构词、消息开头称呼并按消息去重，采集前剥离 reminder/context/reference、附件/技能标记和插件生成提示
  - 验收断言：verifyClaim/verifyFeatures（通过/越界/未知指标/缺 claim/空条目）；ratioPhrase 档位措辞（<10% 偶尔 / ≤40% 有时候 / >40% 常）
  - 提示槽：describeBaselineForPrompt 按通道输出统计说明、buildChannelPrompt 含语料+提示槽+指标白名单+指令词禁令+验收反馈注入
  - 分通道：五路定义、parseChannelJSON 容错（代码块/杂字/非法）、clipFeatures 去重限条、distilChannel（一次通过/带反馈局部重试/重试耗尽 weak 保留通过特征/全失败 failed）
  - 合并：diffTemplateFeedback 用户修订 diff、formatFeedbackBlock 反例/锁定约束、buildMergePrompt 带实测占比、mergeTemplate（合规/含 locked 时缺缺失拦截重试/全失败）
  - 终检 checkStyleDraft 强化：格式+指令词+绝对化词上限+locked 缺失拦截
  - 状态机 v2：档位×通道（light 2 路/balanced 4 路/deep 5 路）、runStyleTask 全链路（采样→基线→通道→合并→画像快照落盘）、单路瞬时报错/非 JSON 局部重试、全部通道 failed→failed、排除名单、语料跨助手累计、画像 profile 落盘、重启恢复
  - 存储：profile 默认空值/写入回读/归一化去重；feedback 读写/超限截断尾保；mergeDiffIntoFeedback 删→反例增→锁定、去重、源标记
  - API 接线：/api/style-profile 画像+反馈查询、confirm 时 diff 沉淀（首版确认不误沉淀）
  - 任务恢复显示（v0.33.83）：最新任务状态优先、失败原因持久显示、待保存草稿不被旧任务冒充；`GET /api/style-template` 保留 `confirmed` 并通过隔离子进程实链路验证
  - 选定文本模型调用（v0.33.84）：Hana 配置的 provider/model 不再静默落到全局 utility；DeepSeek Responses 使用 `reasoning.effort=none`，Chat Completions 使用 `thinking.disabled`；正文提取隔离 reasoning、完整/混搭/未闭合隐藏块清洗、空正文统一错误与超时/接口路径回归
  - 合并终检重试（v0.33.98）：重试切换为独立校对提示，携带上一版草稿和机器标记词做最小改写；同时拦截编辑角色泄漏、模型自行换算的具体次数/百分比、未成对中文引号，并允许整数百分比的显示舍入误差
- 识图 prompt：知名角色/梗图/系列报名字规则（虹夏/月薪喵/熊猫头/猫meme）、防幻觉特征核对、易混淆对照表拼接（爱音 vs 波奇）
- 教学样本（用户教一次、以后识别更准）：向量匹配阈值、命名去重合并（含描述名字候选提取）、样本上限淘汰最旧、upsert 覆盖更新、embedding 失败静默跳过、删除样本；教学名单注入（最近 30 条按时间排序、名字+特征分离、无样本不注入）
- 数据迁移与恢复（v0.26.1）：新位置已有数据不动、插件目录旧位置复制、安装备份最新优先恢复、无效备份跳过、恢复后幂等
- 已删除方言（闽南话）残留人格块清理（回归：normalize 过滤后 remove 分支拿不到）

新增或修改以上逻辑时，应先补对应失败用例，再修改实现，最后运行 `npm test`。

## 手动实机验收（需要真 Hana 环境，jsdom 测不到会话原图和隐藏上下文）

- 悬浮球原图链路：重启 Hana 后发送有正文和无正文两种表情包，确认聊天按附件暗号显示原图、可见正文不含标题标签，助手能按隐藏语义理解；若登记失败才观察原生 images 兜底，保留日志和对应会话 JSONL 再排查。
- express 原图链路（Hana 0.679+）：重启 Hana 后调用 `express`，聊天里应直接出现实际表情包图片，不再只出现「WebView / Chalkboard」占位卡；旧版宿主按版本分流继续验证 iframe 卡片，未知版本也应保守回到旧卡片；历史 iframe 卡片仍可单独回放验证 `hana.ready` 与 `ui.resize`。
- 纸飞机配图反馈链路：助手发图后切到对应对话，纸飞机尾流闪亮一次，面板显示最近配图；喜欢/不喜欢可切换或撤销，状态不串会话；点「和小花聊一聊」后能多轮对话、看到标签修改建议，只有点「确认应用」才修改标签并重算向量。
- 一键搬家包：在「数据与迁移」页用「选择文件夹…」或手动路径导出，确认 ZIP 生成；把导出的 ZIP 在另一套图库导入，检查图片数量、名称/描述、情绪/场景/关键词/氛围标签、助手偏好、教学文字样本、应景/曝光记录、学我说话资料和相关设置都恢复，且同图会复用目标 ID、教学向量按目标模型后台重建；再从图库导入没有 `stickers.json` 的普通旧 ZIP，确认仍进入待识图流程；取消原生选择框后确认路径和最近目录配置不被改写。
- 说明：新版前端把 `plugin_card` 统一当作 Chalkboard/WebView 入口，主聊天要显示文件原图必须走 `details.media.items`；本次不绕宿主内部实现。
