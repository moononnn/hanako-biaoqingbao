## v0.33.73 · 2026-08-25

### 应景分析与情绪感知

- 应景渠道情绪分析兜底加固：思考型模型（MiniMax-M3 / deepseek-v4-flash）在 250 token 上限下常把输出配额全花在思考上，正文只剩 1 token，宿主剥掉思考后报「模型未回复正文」导致约 33% 情绪分析失败；maxTokens 提升到 800，并对这类概率性失败（空正文 / 超时）自动重试一次（纯函数判定，确定性错误不重试），补回归测试 4 项。

### 配图卡片显示

- 内测版 0.686.15 自适应失效修复：新宿主纯 card iframe 分支的 size 读取被旧分支条件挡住，aspectRatio 恒回退 400:430 大白卡；现无条件读取图片尺寸，并让 iframe 兼容 devkit 1.0 裸 ready/ui.resize 消息（双发幂等）。
- 实测确认宿主聊天流内联插件卡片 frame 为 width:100% + aspect-ratio 定高，ui.resize 在聊天流内不再生效（仅 Chalkboard 卡片有效）；自适应语义改为「智能开一律放大填满」（回到 v0.24 直觉），aspectRatio 按放大后高度算；关智能才回退旧阈值行为（≥200 放大、<200 原尺寸防糊）。
- 发图卡片标题改为「主情绪 + 小表情来啦」，优先取图片的情绪标签，过长或缺失时回退为「小表情来啦」。
- 发图卡片移除表情包描述标题和插件元数据行，只保留图片与反馈按钮；图片本身内容不变。

### 配图反馈

- 主对话框配图卡片反馈改版为「喜欢 / 应景 / 不喜欢」三键（对齐纸飞机悬浮球已落地的新版反馈系统）：喜欢/应景可独立、叠加成 both、再点同维度取消；不喜欢才出现「聊一聊」；提交改走 /api/ball/feedback（支持快照撤销与 both）。
- 0.686+ 宿主（devkit 1.0 迁移）不再为白名单外插件工具生成 media_generation 占位块，express 走 deferred 广播必然漂移双图并跟随后续轮次；0.686+ 改走 media-only（details.media 直渲），旧版保持 deferred/卡片链路，并补版本分流回归测试（旧版前向兼容不受影响）。
- 0.686.15 复现表情包图漂到消息流末尾、随后续轮次反复出现（老 bug 复发）：deferred:resolve 在工具内同步执行会早于宿主挂载占位块，渲染端找不到可替换块就把图追加到流末尾；改为 register 后延迟 1.5s 再 resolve，对齐原生生图「先挂占位、后原地替换」的时序。
- 纸飞机正反馈拆两键：喜欢/应景一键直达（不再弹下拉菜单），“两者都是”并入两键自然组合；已点维度再点=取消；聊一聊改回点不喜欢后才出现（最近配图+配图手帐同步）。
- 配图手帐反馈失败后重新启用当前行按钮，避免一次网络失败锁死反馈入口；配图手帐加载加入请求代次，旧后台回包不再覆盖重新打开后拿到的新列表。

### 曝光与偏好

- 新图与未曝光图探索轮换、独立曝光账本（含历史迁移与成功送达记账），以及纸飞机喜欢/应景/两者都是正反馈分层。
- 曝光历史迁移跨 decision-log 与 recent-match 去重，升级旧版账本并校正现有重复计数。
- 共享 mapping 被复用后撤销只回滚当前图片；失效快照返回可见冲突，避免误删其他会话偏好。
- 兼容极老格式根级偏好，新增助手反馈桶后不再静默丢失旧偏好；场景正反馈 clear 改为单次递减，避免跨会话撤销时整条清空。
- 串行化 express 决策日志读改写，避免并发调用互相覆盖历史记录。
- 管理页偏好区新增「应景」反馈：决策日志应景按钮、映射区「很应景」记录展示与单条移除、清理失效时同步清理应景账本。

### 识图与入库

- 纸飞机识图确认入库补写 tagged_at，并为现有 35 张已识图记录回填时间，避免图库误显示「未识图」。
- 启动时为已有标签但缺少 tagged_at 的旧图片按 added_at 自动回填，并补回归测试。

### 稳定性与其它修复

- LocalIpcServer 调 handler 时未解析 query 参数，/recent-matches 的 limit 永远走默认值 10（静默失效）；现解析 query 传入 handler，并新增 ipc-server 回归测试 5 项。
- 删除表情包时同步移除所有会话的最近配图死记录，避免手帐残留不可操作条目。

## v0.33.50 · 2026-08-23

### 纸飞机悬浮球与配图手帐

- 打开插件页面后自动启动纸飞机悬浮球，手动关闭后本次运行不再自动弹出；右键可刷新表情包或关闭入口。
- 悬浮球固定为纸飞机，加入连续巡航、气体尾流、流星背景、悬停与点击反馈；面板支持拖动、窄屏收缩、拖拽排序和 GIF 动图播放。
- 新增拖拽/粘贴图片识图入库：支持 GIF 原图保留、预览播放、标签编辑、教学样本和可选自动生成语义索引。
- 新增按公开对话绑定的最近配图手帐，展示对话标题、最近图片和喜欢/不喜欢/聊天反馈；记录按会话隔离，旧格式自动迁移。

### 配图交付与稳定性

- 新版 Hana 走原生图片 deferred 交付，修正新前端配图显示问题：图片固定在当前回复并保留历史归属，不再套 iframe 卡片；旧版继续回退到 iframe 卡片。
- 注意：新版 Hana 前端暂不支持配图卡片下方的「喜欢/不喜欢」按钮，配图偏好反馈请走悬浮球的反馈路线；旧版 iframe 卡片照常显示按钮。
- 修复悬浮球原图发送、会话标题、GIF 字节识别、剪贴板动图、删除图片后动画崩溃、异常退出自动重启和反馈并发校验等问题。
- 收紧本地运行时文件排除规则，插件数据继续使用 Hana 官方数据目录，不随仓库发布。

### 测试

- Node.js 内置测试 196 项全绿；Python/PyQt6 离屏测试 69 项全绿。

## v0.33.3 · 2026-08-20

### 配图卡片：根绝滚动条（长 toast 不再撑出横向滚动条）

- 实机回归：点喜欢/不喜欢的 toast 长提示是 `position:absolute` + `white-space:nowrap`，一显示就横向溢出 iframe，把横向滚动条顶出来。
- 修：iframe 内 `html, body { overflow: hidden }` 根绝横/纵滚动条（内容必须贴合上报尺寸，超宽只裁不滚）；`.fb-toast` 改限宽折行（`max-width:100%` + `white-space:normal` + `line-height:1.4`），不再硬撑超宽。
- 浏览器实测：修复前 body scrollWidth 289>139 出滚动条，修复后 scrollWidth=139、无滚动条、toast 不越界。

## v0.33.2 · 2026-08-20

### 配图卡片：反馈按钮横排最小宽（按钮不再被小卡挤变形）

- 实机回归：卡片自适应小图后，「喜欢/不喜欢」被挤成竖向/变形。根因是图片加载时按钮区尚未布局（offsetWidth=0），宽度只按图宽上报，宿主把 iframe 开太窄。
- 修：按钮可见时卡片最小宽度 = 能横排放下两枚按钮（~130px 兜底，取与实测按钮区宽的较大值）；按钮加 `flex-shrink:0` + `white-space:nowrap` 永不换行；图片加载后等两帧再量按钮区尺寸；隐藏按钮时仍纯贴图。

## v0.33.1 · 2026-08-20

### 配图卡片：自适应二分规则（废弃四档，回到直觉自适应）

- 自适应规则调整：废弃 v0.32.1 的四档智能封顶（160/280/480/640），卡片自适应回到「阈值二分化」（等价于把仓库版 v0.24 的 200px 硬线改成 400px）：
  - 短边 < 400px：保持原尺寸、卡片紧贴原图（不放大防空糊）；
  - 短边 ≥ 400px：一律放大填满 400（宿主槽位宽上限）。
- 一句话：目标显示宽 = min(图片短边, 400)，卡片与内容同宽，所见即所得。
- 宽度上报改为贴合内容真实宽（图卡外宽与按钮区实际宽取大者，去掉旧 +26 冗余），画面左右对称；隐藏「喜欢/不喜欢」按钮后卡片只剩图片本身。
- 决策抽到 `lib/smart-fit.js` 的 `fitDecision`（二分）+ `AUTO_FIT_MAX=400`，前后端同一份；新增二分回归测试，关闭开关仍回退 v0.24 旧行为（阈值 200）。

## v0.33.0 · 2026-08-20

### 配图卡片尺寸上报：协议根因修复（小图套大卡）

- 实机定位「小图套大卡」真根因：插件发的尺寸事件名写成了 `hana.ui.resize`，而宿主（在 `hana.plugin.ui` 协议下）只认 `ui.resize`，消息一直被忽略，卡片永远停在 400 宽大槽位、小图挤在左上角。
- 改为发送 `ui.resize`，并对齐宿主 card 槽位口径（宽 50~400 / 高 30~600），宿主收到后会把整卡随图收缩。
- 新增 `lib/image-size.js`：零依赖读图头拿宽高（PNG/GIF/JPEG/WebP/BMP）；发图时按图片尺寸动态算初始 aspectRatio（宽恒 400、高度贴合图片+按钮预留），避免初始先闪一个大高框。
- 更新协议回归断言（必须发 `ui.resize`、旧错误写法必须不存在）；新增 image-size 测试用例。

## v0.32.2 · 2026-08-20

### 配图卡片：档位宽度改为 iframe 内兜底（实机回归）

- 实机发现宿主不响应 `hana.ui.resize` 的 width 收窄：iframe 按 aspectRatio 固定约 400 渲染，配图卡片原本靠外层收窄的档位全被抹成 400 满宽。
- 改为给图片直接设内联目标宽（内联样式优先级压过 `.fit` 的 width:100%）：外层固定 400 时，100px 微图显示 100 居中、200px 图显示 280，档位差异在 iframe 内部就可见；宿主若响应 width，外层仍会再收窄一次，双保险。

## v0.32.1 · 2026-08-20

### 配图卡片：智能多档自适应

- 把 v0.24.0 的「小图自适应」一条 200px 硬线升级为智能多档（默认开启，用户零操心）：
  - 短边 <160px 的微小图：保持原尺寸不放大，防糊，卡片紧贴原图；
  - 160~480px 的中小图：放大但显示宽度封顶 280px，200px 左右的图不再被硬拉成海报尺寸；
  - 480~1024px 的中图：封顶 480px，清晰又不满屏；
  - ≥1024px 的大图：封顶 640px，够看也不撑满主播卡片整宽。
- 图被档位封顶时卡片同步收窄贴图（不再留一大片白边），按钮宽度下限仍保证不挤压喜欢/不喜欢。
- 关闭开关回退到旧行为（大图放大填满、小图原尺寸），可随时逆。
- 档位表抽到 `lib/smart-fit.js`，前后端共用同一份常量，新增 `tests/smart-fit.test.js` 覆盖各档与回退。

## v0.31.0 · 2026-08-18

### 表情包悬浮球（施工版）

- 悬浮球开关移到插件主页，和「闲不住」一样只负责开关，不再塞进模型设置弹窗。
- 图库每张表情包增加「加入悬浮球 / 已加入悬浮球」操作，优选内容与启停控制分开。
- 修复悬浮球面板加载时漏导入 `QTimer` 的问题，并补充 Python 进程错误回显，启动失败时不再只显示空状态。
- 当前仍需真实 Hana 端到端验收；临时 POC 路由按施工顺序暂留，验收通过后清理。

## v0.30.12 · 2026-08-18

### 表情包悬浮球（施工版）

- 新增桌面小猫爪悬浮球：可从个人优选表情包中选图、输入文字，一起发到当前最活跃会话。
- 新增本地代理鉴权、图片预览、pinned 配置、启停状态和 PyQt6 离屏测试。
- 发送继续沿用已验证的图片暗号 + 表情包语义块，不在悬浮球里额外做识图。
- 当前仍需真实 Hana 端到端验收；临时 POC 路由按施工顺序暂留，验收通过后清理。

## v0.30.11 · 2026-08-16

### 修复

- **方言保存不生效（新版本 Hana）**：Hana agents 结构迁移后人格文件从 `ishiki.md` 改为 `AGENTS.md`，方言配置保存成功但重启后仍用旧文案。现在自动识别新结构写入 `AGENTS.md`，旧结构 Hana 自动回退 `ishiki.md`，新老版本均不受影响

## v0.30.10 · 2026-08-14

### 命名统一

- 方言展示名统一为「学我说话」：方言列表、设置页标题、保存提示、README 全部用固定名，不再按用户名动态拼名（移除之前的「XX话」/「我的风格」动态展示逻辑）

## v0.30.9 · 2026-08-14

### 🎙️ 学我说话：让助手模仿你说话

- **新方言「学我说话」**：手动总结你自己的发言习惯，生成可编辑的风格模板，让助手模仿你说话（复用内容分析模型与后台任务，模板可编辑、可回退）
- **深度档两阶段蒸馏**：全部语料分块提炼要点 → 要点 + 采样综合成最终模板（所有聊天记录都会被模型看到）；生成后规则自检（字数/身份化/零指令词/无 markdown），不通过自动重生成最多 3 次
- **免选择流程**：默认采集全部助手语料；「从哪些助手学」可展开排除列表（默认全勾，取消勾选即排除并记住）
- **一键套用**：「用它！」= 保存模板 + 自动给所选助手配上「学我说话」方言 + 提示重启，不用再手动衔接；独立「只保存模板」按钮
- **展示名固定为「学我说话」**：不再按用户名动态拼名（读不到用户名也不影响理解）
- **历史版本简化**：只保留最近一版，可逆回退（再点一次换回来）
- **草稿体验**：实时字数计数，超 600 字红色提示并禁用确认；超长草稿「自动精简」按钮一键压到 550 字内（分享版用户不用手动删）
- **语料预算控制**：二次抽样最多 400 条 + 总字符预算 6 万，超大数据不再撑爆模型接口；模型调用失败自动重试一次

### 修复

- **学我说话模板保存后不生效**（回归）：保存/回退/清空只更新模板文件，已开启「学我说话」的助手人格文件里还是旧模板；新增自动同步，保存时自动重写/移除已配置助手的人格块，保存提示同步显示同步结果
- **总结机制重构**：修复重大 bug——语料文本从未插入提炼 prompt，模型一直在凭空编风格（表现为模板不像本人）；改为完全重新生成（增量会把旧模板当用户确认内容全保留、只加不减），旧模板靠历史版本回退兜底；语料跨助手累计
- **提炼浓度修正**：prompt 增加「浓度与分寸」要求（区分常用/偶尔、示例有反差、稀释过浓旧模板），温度 0.7→0.5（回归：第一版模板把点缀式卖萌提纯成每句卖萌）
- **草稿字数硬约束**：提炼 prompt 增加 450-550 字与一段话格式要求（回归：模型生成 864 字 markdown 分段草稿超上限）
- **总结档位改三等高亮按钮**（回归：btn-primary 自带宽度撑满）；任务列表接口补 draft 字段（回归：「总结完没反应」）
- 学我说话页面细节：删除「再总结一次」「放弃」按钮、精简布局、删除「去配方言」跳转按钮、总结状态提示移到按钮下方

### 🗣️ 北京话加强版精修

- 新增「您」后置句式（吃了吗您 / 歇着吧您内）、「呢还」倒装（还没吃呢还）、口头禅系统（可说呢 / 谁说不是呢 / 好家伙 / 您猜怎么着）、打字叠词习惯（行行行 / 得嘞得嘞）与高频儿化词表（今儿 / 事儿 / 地儿 / 弯儿等）
- 回响例句池扩到 8 句（倒装/后置句式占多数：干嘛呢您 / 歇着吧您内 / 走您嘞），人格文案句式段改对照式写法（普通话怎么说 → 北京话怎么说），激活模型句式意识

### 🛠️ 稳定性

- **「焊死门」**：开启/关闭方言写人格文件时默认联动更新配置与日志，杜绝配置与人格漂移（曾出现手动开启后配置停留在旧方言的问题）；并修正存量漂移

## v0.29.0 · 2026-08-11

### 安全修复

- **堵住方言配置的路径穿越漏洞**：保存方言配置时，助手 ID 现在只接受字母、数字、下划线、横杠。以前 `../`、`..\` 这类写法能绕过检查（`..` 会被当成“上一级目录”），把方言文件写到 Hana 安装目录之外，甚至顺手创建目录；现在一律过滤，另加一层最终路径校验兜底，双保险（含 Windows 尾随空格绕过防护）

## v0.28.0 · 2026-08-08

### 配图卡片随心开关

- **新增「显示反馈按钮」开关**（偏好设置 → 配图卡片）：聊天里每张配图下方那排「喜欢 / 不喜欢」按钮，不想要可以直接关掉，卡片就只剩干干净净一张图；想重新调教配图时随时打开
- **卡片高度自适应**：按钮关闭时卡片贴合图片尺寸，底部白边消失；按钮打开时按按钮区实际高度自动放大，不多不少
- **按钮组更轻盈**：去掉喜欢/不喜欢外面的容器框，按钮直接贴在图片下方

### 修复

- 显示配置改为合并式保存：只改动一个设置项时，不再覆盖其他配置

## v0.27.2 · 2026-08-06

### 配图卡片重新选择

- **每次出图都重新选择**：之前点过喜欢/不喜欢的图再次出现时，按钮不再预置亮灯（不再替你做决定），初始都是未选择状态，点了才算数；历史态度只留一句提示（「这张之前记过喜欢/点过 N 次不喜欢，可重新选择」）
- 喜欢/不喜欢行为对称：喜欢有视觉反馈（按钮亮起），不喜欢照常累计次数 + 弹「和小花聊聊」

## v0.27.1 · 2026-08-06

### 反馈入口

- **首页右上角新增「反馈」按钮**（检查更新旁边）：遇到 bug 或有建议，点一下直接打开 GitHub Issues 页面，弹窗被拦时会自动复制链接
- **README 新增「反馈」小节**：说明在哪里提 issue、提的时候带什么信息，装之前就能找到

## v0.27.0 · 2026-08-06

### 方言 × 表情包联动

- **方言开启后，选图/搜图自动带地域气质**：东北话爱选整活炸裂的、上海话偏嗲糯婉转的、粤语幽默中带甜……9 种方言各有 6 大类情绪（搞笑/开心/难过/无语/感谢/鼓励）的气质系数表，命中对应情绪标签时加权，同分数下方言气质对味的图优先出场
- **加强版气质更浓**：加浓开关开启后，气质系数偏离中性的部分翻倍，方言味更明显；普通档保持轻口
- **无需新开关**：方言开着就自动生效，关了就完全不干预选图
- **情绪词归类抽成公共模块**（lib/emotion-groups.js）：具体情绪词统一归到 6 大类，选图与搜图共用同一口径，不会两边算岔

### 修复

- **配图卡片「不喜欢」按钮**：历史点过不喜欢的图再次出现时，再点会被防重拦截（只冒气泡、不累计次数、弹不出「和小花聊聊」入口）；已改为每次出图独立计算，可重新表达——不喜欢正常累计降权、聊聊入口正常弹出

## v0.26.1 · 2026-08-04

### 🔧 紧急修复：图库数据不再因更新插件而丢失

- **数据目录迁移**：图库数据（stickers.json、图片、偏好、向量、教学样本等）从插件目录内迁到 Hana 官方数据目录（`~/.hanako/plugin-data/biaoqingbao/`）。之前数据在插件目录里，更新插件时 Hana 会整目录替换，辛苦存的图库会被清空
- **自动找回旧数据**：更新到本版后，插件启动时自动检测并恢复：① 插件目录旧位置的数据直接复制过来；② 如果已经更新过插件（旧目录被替换），从 Hana 安装插件时自动生成的备份（`~/.hanako/plugin-backups/biaoqingbao/`）里恢复——**已丢失的图库也能找回来**
- 数据只复制不删除：旧位置和备份原样保留，绝不删用户任何东西

## v0.26.0 · 2026-08-04

### 识图认角色 & 教学系统（v0.26.0 新功能）

- **知名角色/梗图/表情包系列直接报名字**：识图新增名角规则，虹夏、月薪喵、熊猫头、猫meme 这类名角名梗直接报名字（外观描述词同时保留，搜名字搜描述都找得到）
- **防认错**：报名字前先核对至少两个独有特征（发型/瞳色/服装/系列格式），对不上只写外观不瞎猜；内置易混淆对照表（千早爱音 vs 后藤一里这类粉毛撞车），后续可扩展
- **教学样本：用户教一次、以后识别更准**：手动改标签（编辑器或聊天式确认）时记下「这张图长什么样+用户叫它什么」（文本向量），之后识别新图自动对比，命中就把用户教的命名并进关键词（含从描述提取名字候选，只改描述也算教学）；上限 200 自动淘汰最旧，删图同步清样本，embedding 未配置时静默跳过
- **教学名单注入**：识别前把用户教过的命名（最近 30 条，名字+特征）随识图 prompt 喂给视觉模型对照——模型看图认角色，同角色不同表情也能直接报名字（呆猫八条戴蝴蝶结纯真 / 眯眼持刀坏笑都认得出）

### 方言加强版全面升级（九种方言配齐）

- **九种方言全部配齐精修加强文案**：之前只有四川话有精修，现在东北话、河南话、上海话、粤语、台湾腔、陕西话、北京话、新疆话全部补上（语气词情绪表 + 程度补语 + 特殊句式 + 正事示例），家家有真功夫
- **句式段改为对照式写法**：普通话怎么说 → 方言怎么说（如「正在看」→「有在看」、「吃过饭了」→「饭吃了」），模型直接抄句式结构，不再只是换词
- **二轮深挖调研增量**：按“音调 / 情绪 / 倒装语序”标准为九种方言补充语料（四川话得不得、东北话四字格状态词、河南话得劲情绪轴、上海话格么/三字经、粤语双宾语句序/中英夹杂、台湾腔有在V/穿看看、陕西话吗字系统、北京话您呐收尾、新疆话把字句/动词加给）
- **方言加浓回响强化**：回响升级为双句示范（句式 + 情绪各一），注入频率 40% → 60%，句式浓度明显拉得起
- **新疆话普通版升级**：彩蛋模式（偶尔蹦词）废弃，改为标准整句习惯式文案，内置「饭吃了么？」「这事儿歹得很！」「走撒，吃拌面去」整句示范——调研证实新疆话有真实语序特征（论文背书），不是模型不行，是文案太保守
- **新疆话保留「效果一般，建议开加浓」提示**：普通版实测效果一般（模型语料少），诚实标注并引导开加浓；其余方言不标注
- **方言页 UI 细节**：加浓开关旁新增 token 提示（「开加浓每轮多费一点 token」），只在开启后显示，位置在开关左边、两个按钮挨着

### 上传体验

- 粘贴图片成功后自动隐藏「导入 ZIP」按钮，只留「导入图片」，界面更清爽（清掉粘贴后恢复显示）
- 修复从群/聊天软件复制的动图（内容 GIF 但扩展名 .jpg）导入被拒：扩展名校验失败时按真实签名识别格式入库，动图照常动
- **批量识图结果网格修复**：图片名称优先显示识别出的标题（粘贴导入的图不再显示「粘贴图片」）；编辑区确认按钮统一为「应用」（去掉易混淆的「保存」，编辑入口保留）；后台任务角标聚合所有任务的待应用/失败总数（多张分别识别不再只显示第一张）；任务处理完但状态未切换时直接渲染结果图（不再卡在空进度条等重开）

## v0.25.0 · 2026-08-04

### 配图卡片与多轮偏好

- **喜欢/不喜欢改为累计机制**：不再一次点过就永久锁定，每次出现都能重新表达；不喜欢按次数累计降权（1 次轻降频、2 次明显降频、3 次基本不出现、5 次以上彻底出局），点喜欢清零次数；同时修复拉黑/不喜欢的图可绕道语义向量通道复出的漏洞
- **变心可重新表达**：先点喜欢再点不喜欢再点喜欢，不再被「已点过」拦截，按钮视觉即时更新（失败自动回滚）
- **「不喜欢」后出现「和小花聊聊」入口**：点「聊聊」卡片原地展开聊天（小图+气泡+输入框，后端同一会话），小花给标签建议时直接显示修改前后对照 + 确认修改/再看看，当场改标签并同步重算向量
- **卡片「小图自适应」开关**：短边 ≥200px 的图始终自动放大填满；更小的图由开关决定，开=放大填满、关=保持原尺寸不糊；开关移到图库页改为拨动式，保存即生效
- **卡片宽度自适应**：关闭小图自适应时卡片包着图，下限取按钮区实际宽度，不挤压喜欢/不喜欢按钮
- 偏好页决策日志按钮同样即时变色 + 防重复；不喜欢累计次数的图单独显示 ×N，可一键清除

### 上传与批量识图

- **并发上传**（一次 5 张，后端串行写队列防竞态）+「上传后自动识图」勾选，图片与 ZIP 导入后自动创建识图任务并弹进度窗
- **支持选整个文件夹导入**：自动扫出里面的表情包图片，非图片文件静默跳过并提示
- **支持 Ctrl+V 粘贴图片**：两步引导（先点粘贴区再粘贴，未点框会提示），粘贴后直接显示图片预览，再粘贴替换当前这张，关闭弹窗后重置
- 上传弹窗按钮统一绿底风格（文件/文件夹/ZIP 并排），选完有反馈；没选图时导入按钮禁用；关闭弹窗完整重置
- 粘贴图片成功后自动隐藏「导入 ZIP」按钮，只留「导入图片」（清掉粘贴后恢复显示，想导 ZIP 随时可以）
- **批量识图重构**：单任务上限放宽到 1000 张、进度弹窗瘦身（大进度条 + 正在处理缩略图）、完成后统计 + 一键全部应用 + 结果网格（点开可编辑）、单张识图改当场识别当场应用（不再绕后台任务）、后台任务角标简化（无任务隐藏，点击直达进度/结果）
- 批量识图任务处理完就退场；重试成功后自动清除旧任务的失败记录；角标数字只反映点开后看到的内容；任务全部处理完自动关弹窗

### 助手管理

- 配图频率页新增「删除助手」和「刷新列表」：删除 = 清理该助手的配图频率/偏好/方言/人格块数据并从当前列表移除；刷新 = 重新扫描 Hana 当前助手列表，所有助手（含刚删的）都会重新出现，新助手无需重启 Hana 即可出现，自由度交给用户

### 方言加强版（开关式常驻）

- 方言加强版改为常驻开关：所有方言都能开，开启后每轮对话注入方言回响（正事场合自动让路、会话前 8 条必注入之后 40% 概率衰减省 token），四川话保留精修文案，旧 mode 配置自动迁移为 boost

### 修复与稳定性

- 修复配图卡片喜欢/不喜欢切换时按钮视觉不更新（同方向防重逻辑误拦「变心」）
- 修复柴犬动图（stk_315）不播放动画：全库唯一缺 NETSCAPE 无限循环标记的 GIF，已补上（原文件已备份）
- 修复批量识图任务「处理完就退场」：成功且应用的项不再出现在结果网格；修复「显示 8 张失败但点进去只有 1 张」的角标虚报
- 修复单张识图写回前不重读最新数据导致的操作覆盖；编辑器「AI 重新识图」改预览模式（不落库，点保存才生效）
- 修复 add_sticker 工具多会话并发撞 id：分配 id → 复制 → 写元数据整体进串行队列
- 修复向量检索就绪判断硬编码模型名（非 bge-m3 模型被误报未配置）；hana 模式保存的维度改用连通测试返回的真实维度，custom 模式测试成功后自动填充维度
- 修复搜索候选列表不吃偏好惩罚（与选图口径一致，不喜欢的图不排前面误导助手）
- 修复从群/聊天软件复制的动图（内容 GIF 但扩展名 .jpg）导入被拒：扩展名校验不过时按真实签名识别格式入库，动图照常动
- 修复「不喜欢后弹窗」交互细节（聊聊入口只在点过不喜欢后出现）
- **发布前审查修复（五）**：全部前端请求补超时（识图/聊天 90s、embedding 60s、批量任务轮询 10s、普通 API 15s，慢请求不再挂死按钮和 loading）；卡片初始反馈状态同步防重标记（之前点过的图再次出现，重复点击正确拦截）；聊天标签确认接口增加会话归属校验与字段白名单；配图反馈工具校验 sticker 存在性与反馈类型；上传与 ZIP 导入互斥、单次导入上限 200 张；ESC 关闭聊天弹窗清理后端会话；清理 15 个未使用 import

## v0.23.0 · 2026-08-03

### 方言口音（精简 · 实测拍板）

- **删除闽南话**：实测模型无整句闽南方言打字语料，彩蛋模式也基本没效果，直接删掉（配置里已选闽南话的助手会自动归为未配置）
- **去掉难度标注**：不再给各地方言标注「效果好/效果一般/效果较弱」，UI 下拉和预览句恢复干净
- **新疆话保留「效果一般」提醒**：唯一一处难度标注，用来提醒装了插件的其他用户降低预期
- **文案升级：起头锚定 + 质量声明**（融合外部方案，实测拍板）：每种方言新增应答词坑位（openers），文案改为「接话时爱用X起头，顺手用X替换，句尾偶尔落个X」，给方言明确使用位置，钉住每轮第一句；末尾加质量优先级声明「方言只是你的措辞，不影响内容的质量与严谨」，正事不降智的保险丝；新疆话彩蛋文案也带质量声明
- **北京话新增「不儿」**：混入标志词平级出现，不强制
- **Bug 修复：关闭方言后人格文件不删除**：sync 的待处理名单只能拿到“新配置 ∪ 读缓存”，但保存时缓存已刷成新配置，被关闭的助手两边缺席，remove 分支永不执行；改为保存时把写盘前的旧配置显式传入（含回归测试）
- **Bug 修复：保存按钮 hover 变转圈**：按钮是 btn-primary，全局 `.btn-primary:disabled` 是 `cursor:wait`，保存成功后按钮 disabled 就转圈；加 `#save-dialect-btn:disabled` 局部覆盖为 not-allowed
- 方言页描述同步更新（不再提效果标注）
- **UI：方言选择交互重设计（单控件）**：去掉「先点开关再选下拉」两步操作，改为一个 pill 选择器——点开自定义下拉面板，选方言即开启、选「(不选)」即关闭；面板样式与整体卡片风统一（圆角、柔和阴影、选中勾、hover 反馈），替换原生 select；新疆话「效果一般」在面板内以小字展示
- 方言库剩 9 种：东北话、河南话、上海话、粤语、台湾腔、四川话、陕西话、北京话、新疆话

### 配图卡片与偏好系统（卡片直连反馈）

- **配图卡片自适应**：图片按卡片尺寸自适应显示（object-fit），不再出现小图配大卡片的违和感
- **卡片直连喜欢/不喜欢**：配图卡片下方直接放喜欢/不喜欢按钮，点击即写入偏好，并预置这张图之前的态度状态
- **偏好记录默认折叠**：偏好记录收成一行，点击展开细节，界面不堆叠
- **偏好界面简化**：移除权重数字与符号标记，只留情绪 + 关键词 + 计数 + 缩略图
- **删除图片二次确认**：删除前弹确认框，防手滑
- **「和小花聊聊」按钮**：改名并拉开间距，粉色系更醒目

### 审查修复（v0.23.0 发布前）

- 版本号同步 0.23.0（manifest/package.json，check-update 依赖）
- 首页 5 卡一行，窄屏自动换行不溢出（原固定 190px×5 在窄窗口被截断）
- 已删除方言（闽南话）残留人格块自动清理（normalize 过滤后 remove 分支拿不到的回归修复 + 测试）
- GET /api/dialect 去掉写副作用（自愈只在 POST 保存后）
- POST /api/dialect 响应剥离 ishiki.md 绝对路径，错误信息路径脱敏
- 方言页文案通用化、前端 version 笔误、openers 回退保护、density none 防御

### 测试

- 方言测试同步：9 种库完整性、难度标注仅新疆话保留断言；全套 32 项全绿

## v0.22.0 · 2026-08-02（未发布）

### 方言口音（开关式）

- **浓度三档 → 开关式**：不再分「带一点/正常/浓度很高」，一个开关，开了就带，关了就不带。老配置三档浓度自动迁移：light/normal/heavy → 开启，none → 关闭
- **习惯式文案**：方言是打字习惯，正事闲聊都自然带出来，不刻意压也不故意秀；零指令词，强调「打字」场景而非口语模仿
- **身份化文案**：改为「你是一个土生土长的X人」，台湾腔写明「中国台湾人」
- **新增 2 种方言**：北京话（打字高频京味词：成、得嘞、倍儿、甭、您、今儿、事儿……不收录「诶呦喂」式表演腔）、新疆话（啥、咋、歹、谝、麻达、阿达西、皮牙子……不收录馕言文式夸张比喻），共 10 种
- **难度标注**：每种方言标注模型表现（效果好/一般/较弱），UI 下拉和预览句展示；弱方言（河南/上海/陕西/闽南/新疆）例句加料，文案按难度引用 2/3 句示范
- **hard 档彩蛋模式**：闽南话/新疆话模型没有整句方言语料（无成熟书面打字体系），硬要求只会全滑回普通话；改为「偶尔蹦几个家乡词」彩蛋文案，要求低反而有惊喜感
- **方言保存日志**：每次保存记录时间、从啥改成啥、变更了哪些助手（data/dialect-log.json，最近 200 条），提供 GET /api/dialect-log 供排查；排查「配置莫名变化」类问题用
- **静默失败修复**：保存时人格写入失败的助手会明确红字提示（此前前端只弹「已保存」，把失败提示吞了）；配置有但人格文件缺块时自动补写（自愈）
- **原子写**：写人格文件改为「先写临时文件再替换」，防止写一半损坏 ishiki.md
- **保存提示升级**：保存后提示「重启 Hana + 建议开新对话框」，避免旧对话框残留旧方言味
- **测试隔离修复**：syncDialectToIshiki 支持 agentsRoot 参数，测试不再污染真实人格文件（此前测试跑完会把真实 ishiki.md 覆盖成测试配置）

### 测试

- 方言测试更新：10 种库完整性（含难度/加料断言）、习惯式文案（身份化/零指令词/打字场景/无表演腔/彩蛋模式）、老配置迁移、开关式读写往返、日志读写；全套 30 项全绿

## v0.21.0 · 2026-08-02（未发布）

### 方言口音（最终方案）

- **用户主动开启才写**：给助手选方言+浓度并保存后，把设定写入该助手的 `ishiki.md`（人格文件），标记块包裹，只动插件自己的段落，不碰用户写的内容
- **关闭即删**：把助手设为「完全不带」或清空方言，保存后自动移除人格块，文件恢复原样
- **任何调整都要重启 Hana 生效**（系统提示词有缓存，UI 有明确提示）
- **浓度三档**：带一点（带点乡音）/ 正常（像本地人打字）/ 浓度很高（地道的本地人），靠文案措辞分档
- **正事锚点**：每档文案都带「讲正事的时候，你会先把意思说明白，方言不挡路」，防止干正事被方言带偏降智
- **8 种方言**：东北话、河南话、上海话、闽南话、粤语、台湾腔、四川话、陕西话
- 首页新增「方言口音」入口卡片
- 废弃对话时注入方案（system 消息通道在 Hana 中会被丢弃，user 尾部注入有纸条感）

### 修复

- **测试不再污染正式配置**：配置路径支持 `BIAOQINGBAO_DIALECT_CONFIG` 环境变量覆盖，测试用临时文件隔离（此前测试曾直接覆盖正式 dialect-config.json，把用户配置静默抹掉）
- 方言扩展不再注册（manifest 移除），无对话注入

### 测试

- 方言测试 16 项：库完整性、三档文案（含正事锚点）、配置归一化、人格写入/移除/幂等、隔离路径；全套 27 项全绿

# 更新记录

## v0.19.6 · 2026-08-02

### 修复

- **配图永远 no_match 的致命回归**：v0.19.5 将向量通道改为「显式接收结果再回填」时，因 `applyVectorScoring` 原地修改并返回同一个数组引用，`scored.length = 0` 会把结果一起清空，导致所有助手 express 配图永远提示「没有找到匹配的表情包」。恢复原地修改语义，并新增 execute 调用契约回归测试。

## v0.19.5 · 2026-08-02

这次更新修复了外部审查发现的几个核心链路问题，并补上了对应回归测试。

### 修复

- **语义搜索真正生效**：修复选图时一个异步调用漏等结果的问题。此前宣传的「标签 + 语义向量」双通道里，语义通道实际没有参与选图，现在向量打分与语义相近命中都已恢复。
- **偏好按助手隔离**：选图时只读取当前助手自己的喜欢 / 反感 / 否决记录，不再把其他助手的偏好合并进来；最近使用去重也按助手独立，每个助手逐渐形成自己的表情语言。
- **数据写入更安全**：图库、向量、偏好、配置等所有数据文件改为「先写临时文件再替换」的写入方式，断电或崩溃时不会留下写到一半的文件导致图库索引丢失。
- **向量重算更可靠**：更换向量模型后重算时，不再把旧模型生成的向量和新模型的混在一起；生成向量时会检查返回的数量、维度是否正确。标签修改后的单条重算在模型已更换时会提示先整体重算。
- **问候词误判修复**：英文问候词（hi / hello）改为按单词匹配，`this`、`while`、`something` 等包含 `hi` 的英文单词不再被误判为问候。
- **批量任务不再卡死**：重启恢复时，如果任务已经处理完全部图片但没来得及标记完成，会直接标记为完成，不再永远停在「运行中」。
- **情绪词清洗**：自动识图与情绪感知返回的情绪词会去除换行、引号等特殊字符并限制长度，防止异常输入影响后续使用。
- **偏好反馈不再丢失**：管理页里的 👍 / 👎 反馈按钮现在会把反馈记到正确助手名下（此前会记到全局默认桶，助手选图时读不到）。
- **删除表情包更干净**：删除图片时同步清理它的向量索引与偏好记录，不会留下孤儿数据。
- **批量任务更稳**：多个后台识图任务同时完成时不再互相覆盖进度；重启恢复时卡在「运行中」的任务会自动收尾。

### 测试

- 新增 4 项回归测试，总数从 4 项增至 8 项：
  - 向量通道能在无标签匹配时靠语义相似度选中图片（拦住少 `await` 问题）
  - 向量 bonus 排序
  - 偏好按助手隔离不串号
  - 英文问候词词边界

### 发布说明

- 公开版本仍为空图库，不包含作者的私人表情包、聊天记录、API Key 或本机路径。

---

## v0.19.4 · 2026-07-30

这次更新重点是给插件补上自动自检，并修复批量识图在重启边缘情况下可能漏图的问题。

### 新增

- 加入零额外依赖的自动测试，运行 `npm test` 即可检查关键逻辑。
- 首批测试覆盖：
  - 日常 / 正事两阶段配图频率
  - 标签匹配、偏好、否决与防重复
  - 批量识图任务重启恢复
  - 已取消任务不会被误恢复
- 新增 `TESTING.md`，记录测试方法与当前覆盖范围。

### 修复

- 插件在批量识图过程中重启时，会把 `current` / `current_ids` 中尚未完成的图片重新放回待处理队列。
- 恢复时会自动去重，并避开已经成功或已经失败的图片，防止重复识别。

### 发布说明

- 公开版本仍为空图库，不包含作者的私人表情包、聊天记录、API Key 或本机路径。
- 插件需要 `full-access`、模型与网络访问能力，具体风险和用途请先阅读 README 开头的提醒。
